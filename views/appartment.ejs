<!DOCTYPE html>

<html>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<title>Add Room</title>
		<meta name="description" content="">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link rel="stylesheet" href="css/style.css">
	</head>
	<body>
		<div id="container">
			<h1>Liste des pièces de l'appartement</h1>
			<ul>
				<% if (rooms.length != 0) { %> 
					<% console.log(rooms) %>
					<% let totalArea = 0 %>
					<% for(let i=0; i<rooms.length;i++){ %>
						<% totalArea += rooms[i].getSurface() %>
						<li id="room">Pièce : <%= rooms[i].getName() %> <%= rooms[i].getSurface() %> </li><br>
					<% } %>
					<li id="total-area">Surface totale : <%= totalArea %> </li>
				<% } %>
			</ul>
			<a href="/"><button>Encoder une autre pièce</button></a>
		</div>
	</body>
</html>